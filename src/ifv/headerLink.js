import React, { Component, PropTypes } from 'react';
import { Link } from 'react-router';
import { connect } from 'react-redux';
import { propTypes } from 'react-props-decorators';

export const headerLink = (
  <HeaderLink name="IFV" path="/ifv"/>
);
